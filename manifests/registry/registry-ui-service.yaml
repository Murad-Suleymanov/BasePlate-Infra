apiVersion: v1
kind: Service
metadata:
  name: registry-ui
  namespace: registry
  labels:
    app.kubernetes.io/name: registry-ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: registry-ui
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
