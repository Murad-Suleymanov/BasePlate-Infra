{{- if .Values.kialiHostname }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kiali-route
  namespace: istio-system
  annotations:
    external-dns.alpha.kubernetes.io/hostname: {{ .Values.kialiHostname }}
    external-dns.alpha.kubernetes.io/target: {{ .Values.targetIP }}
spec:
  parentRefs:
{{- range .Values.route.sections }}
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $.Values.gateway.name }}
      namespace: {{ $.Values.gateway.namespace }}
      sectionName: {{ . }}
{{- end }}
  hostnames:
    - {{ .Values.kialiHostname | quote }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: kiali
          port: 20001
          weight: 1
{{- end }}
